<div class='progress-wrapper'>
    <h1 class='form-title'>Title</h1>
    <div class="progress">
        <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" aria-valuenow="{{ current }}" aria-valuemin="0" aria-valuemax="{{ total }}" style="width: {{ ( current / total ) * 100 }}%">{{ current }} / {{ total }}</div>
  </div>
</div>